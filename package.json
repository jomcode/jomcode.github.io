{
  "name": "jomblog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -k \"browser-sync start -s public -f public\" \"jekyll build -w\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.13.0",
    "concurrently": "^2.1.0",
    "gh-pages": "^0.11.0"
  }
}
